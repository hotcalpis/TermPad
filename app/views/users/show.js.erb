$('#ajax-pagination-left').html("<%= j (render partial: 'shared/post', collection: @posts[0..3]) %>");
$('#ajax-pagination-right').html("<%= j (render partial: 'shared/post', collection: @posts[4..7]) %>");

$('#page_number').html("page_number: <%= @page_number %>");