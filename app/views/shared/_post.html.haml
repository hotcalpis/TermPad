= link_to( post, class: 'list-group-item dictionary-content pt-4' ) do
  %h4
    = post.theme
  %h5.block
    .float-left
      = post.user.name
    .float-right.text-muted
      %time(datatime="#{post.created_at}")= "#{time_ago_in_words(post.created_at)}前"